module Computer
  module System
    # Login module so a user can use their PINS to access the computer
    class Login
      # TODO: add the methods needed so that the tests (specs) pass
    end
  end
end
